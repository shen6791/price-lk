NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
ADMIN_PASSWORD=

# Optional: enables the general web-search layer (src/lib/scrapers/web-search.ts).
# Without these, search still works using only the 4 dedicated scrapers.
BING_SEARCH_API_KEY=
BING_SEARCH_ENDPOINT=
